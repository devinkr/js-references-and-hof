/////////////////////////////////////////////////////
// Convert to Arrow Notation
// Starting function
const isEven = function (num) {
	return num % 2 === 0;
};
// Rewrite the isEven function using arrow syntax in the space below.

// console.log('Is 1 even?', isEven(1));
// console.log('Is 2 even?', isEven(2));

//Starting function
function add(num1, num2) {
	return num1 + num2;
}
// Rewrite the add function using arrow syntax in the space below. Try to write an implicit return! :)

// console.log('What is 3 + 4?', add(3, 4));
// console.log('What is 7 + 5?', add(7, 5));

////////////////////////////////////////////////////
//Functions as values challenge

// Can you create an array and add a function to it in the first index. How would you invoke it?

// Create a function that takes a function as an argument. How do you invoke it?

// Create a function that returns another function. How do you invoke them?
